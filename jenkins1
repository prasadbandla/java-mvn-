pipeline {
       tools{
           maven 'maven'
           }
         stage('Build') {
            steps {
            sh "mvn --version"
            sh "echo $PATH"
            sh "mvn clean install"
            }
        }
       }
